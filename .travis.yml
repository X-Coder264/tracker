language: php

php:
  - 7.2

cache:
  directories:
    - $HOME/.composer/cache
    - $HOME/.app/cache
env:
  global:
    - CHECK_PHP_SYNTAX="yes"

sudo: false

install:
  - cp .env.example .env
  - composer self-update
  - travis_retry composer install --no-interaction --prefer-source
  - php artisan key:generate
  # Install coveralls.phar
  - wget -c -nc --retry-connrefused --tries=0 https://github.com/satooshi/php-coveralls/releases/download/v1.0.2/coveralls.phar
  - chmod +x coveralls.phar
  - php coveralls.phar --version

# Create a storage folder for test coverage report
before_script:
    - mkdir -p build/logs
    - ls -al

# Linting and running tests + generating code coverage report
script:
  - if [[ "$CHECK_PHP_SYNTAX" == "yes" ]]; then php vendor/bin/php-cs-fixer --no-interaction --dry-run --diff -v fix; fi;
  - if [[ "$CHECK_PHP_SYNTAX" == "yes" ]]; then mv ./.php_cs.cache $HOME/.app/cache/.php_cs.cache 2> /dev/null; fi;
  - vendor/bin/phpunit --coverage-clover=build/logs/clover.xml

after_success:
  # Submit coverage report to Coveralls servers
  - travis_retry php coveralls.phar -v
  # Submit coverage report to codecov.io
  - bash <(curl -s https://codecov.io/bash)
